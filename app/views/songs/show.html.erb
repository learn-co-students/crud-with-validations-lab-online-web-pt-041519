<h1><%= @song.title %></h1>

<ul>
<% @song.attributes.each do |a| %>
  <li><%= a.first %>: <%= a.last %></li>
<% end %>
</ul>

<%= link_to "Edit", edit_song_path(@song) %> | <%= link_to "Back", songs_path %> 

<br /><br /><%= button_to "Delete", @song, method: :delete %>